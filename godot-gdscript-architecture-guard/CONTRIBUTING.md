# Contributing

Small fixtures are the easiest way to improve rules. Please include the policy
file, the smallest scripts that reproduce the finding, and the command used.

Run tests before opening a pull request:

```powershell
python -m unittest discover -s tests -v
```

