# Renderer Guidance

For many mobile 2D projects, Forward+ can be unnecessarily expensive. The tool flags it as a warning so developers review whether Mobile or Compatibility renderer is more appropriate.

Renderer choice is visual and project-specific. Treat this as a prompt to test, not as an automatic rule.
