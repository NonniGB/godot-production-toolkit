# Contributing

Validate action metadata with:

```powershell
python -m unittest discover -s godot-release-dashboard-action\tests -v
```

Keep examples copy-pasteable, use project-neutral paths, and avoid adding
project-specific report data.
