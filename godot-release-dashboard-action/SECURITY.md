# Security

This action installs Python packages and reads report files produced by your
workflow. Pin action versions in production workflows and review any custom
`tool-packages` or `extra-args` values before use.

Do not upload signing keys, credentials, private exports, or player data as
dashboard artifacts.
