# Contributing

Useful reports include a small scenario result file, the command used, and the
summary you expected. Keep fixtures synthetic when possible.

Run tests before opening a pull request:

```powershell
python -m unittest discover -s tests -v
```

