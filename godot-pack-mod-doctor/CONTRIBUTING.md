# Contributing

Useful contributions include small manifest fixtures, pack policy checks, and
clearer examples for patch, DLC, or mod workflows.

Run before submitting changes:

```powershell
python -m unittest discover -s godot-pack-mod-doctor\tests -v
```
