# Contributing

Useful contributions include compact dashboard sections for new report types,
better summaries, and small fixture reports.

Run before submitting changes:

```powershell
python -m unittest discover -s godot-release-dashboard-kit\tests -v
```
