# Security

`godot-release-dashboard-kit` reads local report files and writes a local HTML or
JSON dashboard. It does not require network access.

Review public dashboard artifacts before sharing them. Reports may include local
paths, internal project names, or other project-specific context.
