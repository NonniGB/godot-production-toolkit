# Tiny Previous Release Evidence

This folder is a tiny previous-run fixture for the release dashboard example.
Use it with `--previous-reports-dir` to show dashboard trend cards without
needing a real project history.

