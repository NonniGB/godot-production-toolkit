# Security

The tool reads local `project.godot` files and writes reports only where requested.

Input action names can reveal feature names. Review generated docs before publishing them from a private project.
