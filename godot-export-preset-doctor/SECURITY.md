# Security

This tool inspects export preset text files and does not need network access.

Report security issues privately through the repository security advisory flow when the project is published.

Never paste real keystore passwords, signing passwords, API tokens, or store credentials into examples or issues. The report redacts credential values, but users should still avoid uploading reports that include private paths or internal project names.
