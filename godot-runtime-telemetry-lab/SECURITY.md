# Security

`godot-runtime-telemetry-lab` reads local JSON and CSV telemetry files and does
not require network access.

Do not include private player data, analytics identifiers, local paths, or
credentials in shared telemetry fixtures or reports.
