# Contributing

Small, focused improvements are welcome. Useful contributions include sample
telemetry fixtures, extra input adapters, clearer reports, and tests for new
budget rules.

Run before submitting changes:

```powershell
python -m unittest discover -s godot-runtime-telemetry-lab\tests -v
```
