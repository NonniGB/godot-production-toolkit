# Tiny Content Project

This fixture contains a small item and recipe graph. It deliberately includes a
recipe that references a missing item, so the first run has a useful finding.

```powershell
godot-content-graph . --config content-graph.toml
```

