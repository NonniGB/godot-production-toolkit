# Security

`godot-project-doctor` executes configured local tool commands. Review `plan --format json` before running untrusted configs.

Do not include credentials, signing keys, private export paths, or proprietary project data in issue reports. Use minimal fixtures whenever possible.
