# Godot Import Guide

For pixel art:

- Disable filtering.
- Disable mipmaps for crisp UI-scale sprites unless the asset is used at multiple scales.
- Use lossless or VRAM compression only after visual checks.
- Keep generated manifests near source art so seeds can be reproduced.
