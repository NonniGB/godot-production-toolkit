# Asteroid Tiles

```powershell
pixel-space-assets asteroid-hex --material ferric --count 32 --size 64 --seed 7 --output generated\ferric
```

Materials:

- `carbon`
- `ferric`
- `ice`

Each run writes a `manifest.json` with seeds and filenames.
