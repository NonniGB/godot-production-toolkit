# Security

The tool reads local `.gd` files and writes reports only where requested.

Generated reports can include class, method, and file names. Review reports before sharing them publicly if your project contains private identifiers.
